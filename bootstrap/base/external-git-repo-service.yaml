kind: Service
apiVersion: v1
metadata:
  name: external-git-repo
  namespace: openshift-gitops
spec:
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000
selector: {}
