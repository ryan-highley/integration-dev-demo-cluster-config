apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: demo-weather-transform-app
spec:
  destination:
#    namespace: pricelist
    server: https://kubernetes.default.svc
  project: demo-weather-interface
  source:
    path: demo-weather-transform/
    repoURL: https://github.com/ryan-highley/demo-weather-interface
    targetRevision: main
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
