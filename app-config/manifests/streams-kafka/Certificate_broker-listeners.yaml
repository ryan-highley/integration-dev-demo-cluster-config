apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: broker-listeners
spec:
  secretName: cert-broker-secret
  issuerRef:
    name: signer
    kind: ClusterIssuer
  commonName: cluster-kafka
  dnsNames:
  - '*.cluster-kafka-brokers'
  - '*.cluster-kafka-brokers.kafka.svc'
  - '*.cluster-kafka-brokers.kafka.svc.cluster.local'
  - cluster-kafka-bootstrap
  - cluster-kafka-bootstrap.kafka.svc
  - cluster-kafka-bootstrap.kafka.svc.cluster.local
